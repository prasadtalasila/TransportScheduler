defmodule StationFunctions do
  def compute_congestion_delay(delay, factor) do
    # some function using delay and factor values, currently returns delay * factor
    congestionDelay = delay * factor
  end

end
