language: elixir
elixir:
- 1.3
otp_release:
  - 19.1
install:
- mix local.rebar --force
- mix local.hex --force
- mix deps.get
script:
- mix test
after_success:
- mix edeliver build release
- mix edeliver deploy release
- ssh $USER@localhost
- test/ts/ts/bin/ts start