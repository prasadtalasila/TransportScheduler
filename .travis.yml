language: elixir
elixir:
- 1.3.1
otp_release:
  - 19.0
install:
- mix local.rebar --force
- mix local.hex --force
- mix deps.get
script:
- mix test
after_success:
- mix edeliver build release
- mix edeliver deploy release
- mix edeliver start
- curl http://localhost:8880/schedule?stn_code=1
